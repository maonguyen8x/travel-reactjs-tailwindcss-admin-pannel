import { t } from 'app/i18n';

const configContainer = (props: any) => ({
  // title: t('user.title'),
  // breadcrumb: t('breadcrumb.information'),
  // id: props?.match.params.id,
  // onBlock: !props?.userDetail?.blockMessage
  //   ? props?.LOCK_USER
  //   : props?.UN_LOCK_USER,
  // isBlock: props?.userDetail?.blockMessage,
  // tabTitle: t('tabs.title.user'),
});

export { configContainer };
