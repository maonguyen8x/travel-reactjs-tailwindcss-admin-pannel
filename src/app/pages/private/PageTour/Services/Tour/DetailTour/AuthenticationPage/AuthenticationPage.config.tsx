import { t } from 'app/i18n';

const configContainer = (props: any) => ({
  title: t('tour.list_title'),
  tabTitle: t('tour.title'),
  breadcrumb: '#50 DaNangtourist',
  subTitle: t('tour.verify.title'),
});

export { configContainer };
